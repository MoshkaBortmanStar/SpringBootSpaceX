spring:
  application:
    name: SpringBootSpaceX
  datasource:
    url: jdbc:sqlite:src/main/resources/db/spaceX.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
